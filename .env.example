# ──────────────────────────────────────────────────────────────────────────────
# Database — Neon (https://neon.tech) or Supabase (https://supabase.com)
# ──────────────────────────────────────────────────────────────────────────────

# PostgreSQL connection string.
# When set, the app uses PostgresRegistrationStore (persistent, production-ready).
# When unset, falls back to an in-memory store (local dev only).
#
# Neon  — add the "Neon Postgres" Vercel Marketplace integration; DATABASE_URL
#         is injected automatically. Then run: psql $DATABASE_URL -f scripts/migrate.sql
#
# Supabase — copy from Project → Settings → Database → URI, then run the migration.
DATABASE_URL=


# Bearer token required to access GET /api/admin/stats.
# Generate a strong random string, e.g.: openssl rand -hex 32
ADMIN_TOKEN=

# ──────────────────────────────────────────────────────────────────────────────
# Platform service fee
# ──────────────────────────────────────────────────────────────────────────────

# Fee charged on flash loans and swaps, expressed in basis points.
# 1 bp = 0.01% — so 10 = 0.1% (default), 50 = 0.5%, 0 = disabled.
PLATFORM_FEE_BPS=10

# EVM wallet address that receives the platform fee.
# The fee amount is calculated and disclosed in every API response.
# Set this to your operator wallet before going live.
PLATFORM_FEE_RECIPIENT=

